@model CardiologicalResearch.Models.MedicalRecord

@{
    ViewBag.Title = "Delete";
    Layout = null;
}
<script src="~/Scripts/bootstrap.js"></script>
<link href="~/Content/bootstrap-theme.css" rel="stylesheet" />
<link href="~/Content/bootstrap.css" rel="stylesheet" />


@*using (Ajax.BeginForm(
     "Delete",
     "MedicalRecord",
     new AjaxOptions
     {

         HttpMethod = "post",
         UpdateTargetId = "dataTargetField",
         //OnSuccess = "$('body').removeClass('modal-open'); $('.modal-backdrop').remove();"

     }))
{*@
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

<div class="modal-body">
    
    <div class="panel panel-danger">
        <div class="panel-heading" style="text-align:center">
            Заполнение прервано
        </div>
        <div class="panel-body" style="text-align:center">
            Остановить заполнение?
        </div>
    </div>
    
    
    
    
   
    <div hidden="hidden">
       
        <div class="display-label">
            @Html.DisplayNameFor(model => model.MedicalRecordDate)
        </div>
        <div class="display-field">
            @Html.DisplayFor(model => model.MedicalRecordDate)
        </div>

        <div class="display-label">
            @Html.DisplayNameFor(model => model.User.UserName)
        </div>
        <div class="display-field">
            @Html.DisplayFor(model => model.User.UserName)
        </div>
    </div>
    </div>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="modal-footer">
            @*<input type="submit" value="Delete" /> |
    Html.ActionLink("Back to List", "Index")*@
            <button type="submit" class="btn btn-default">Выход</button>
            <button type="button" class="btn btn-default" data-dismiss="modal">Отмена</button>
        </div>
    }
